<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.p2pfileshare.controller.IncomingConnectionController"
            style="-fx-background-color: #EBE1D1;">

    <VBox spacing="20" AnchorPane.topAnchor="20" AnchorPane.leftAnchor="20" AnchorPane.rightAnchor="20" AnchorPane.bottomAnchor="20">

        <!-- Card Header -->
        <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-background-color: #0D4715; -fx-padding: 20; -fx-background-radius: 12; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.12), 6, 0, 0, 2);">
            <Label text="📡" style="-fx-font-size: 28px; -fx-text-fill: #EBE1D1;" />
            <VBox>
                <Label text="Quản lý kết nối đến" style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #EBE1D1;" />
                <Label text="Các peer đang kết nối và xem file của bạn" style="-fx-font-size: 13px; -fx-text-fill: #d6d0c3;" />
            </VBox>
            <Region HBox.hgrow="ALWAYS" />
            <Button fx:id="refreshButton" text="🔄 Làm mới" onAction="#onRefresh" styleClass="button"/>
        </HBox>

        <!-- Bảng kết nối -->
        <TableView fx:id="connectedPeerTable" VBox.vgrow="ALWAYS" style="-fx-background-color: #ffffff; -fx-border-radius: 10; -fx-background-radius: 10; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.06), 5, 0, 0, 2);">
            <columns>
                <TableColumn fx:id="colDisplayName" text="Tên hiển thị" prefWidth="150"/>
                <TableColumn fx:id="colIp" text="Địa chỉ IP" prefWidth="150" style="-fx-alignment: CENTER;"/>
                <TableColumn fx:id="colConnectTime" text="Thời gian kết nối" prefWidth="180" style="-fx-alignment: CENTER;"/>
                <TableColumn fx:id="colPeerId" text="Peer ID" prefWidth="250"/>
            </columns>
            <columnResizePolicy>
                <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
            </columnResizePolicy>
            <placeholder>
                <Label text="Hiện chưa có ai kết nối đến máy bạn" style="-fx-text-fill: #7f8c8d;" />
            </placeholder>
        </TableView>

        <!-- Thanh trạng thái & ngắt kết nối -->
        <HBox spacing="15" alignment="CENTER_LEFT" style="-fx-background-color: #0D4715; -fx-padding: 15; -fx-background-radius: 12; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.08), 5, 0, 0, 2);">
            <Label text="Trạng thái:" style="-fx-font-weight: bold; -fx-text-fill: #EBE1D1;" />
            <Label fx:id="statusLabel" text="Sẵn sàng" style="-fx-text-fill: #d6d0c3; -fx-font-weight: bold;" />
            <Region HBox.hgrow="ALWAYS"/>
            <Button fx:id="disconnectButton" text="🚫 Ngắt kết nối" onAction="#onDisconnect" styleClass="button"/>
        </HBox>

    </VBox>
</AnchorPane>
